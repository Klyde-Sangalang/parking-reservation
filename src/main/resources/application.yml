server:
  error:
    include-message: always
    include-binding-errors: always
spring:
  datasource:
    password: klyde
    url: jdbc:postgresql://localhost:5432/parkingreservation
    username: postgres
jpa:
  hibernate:
  ddl-auto: create-drop
  properties:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show_sql: true
